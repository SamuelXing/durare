[package]
name = "durust-macros"
version = "0.1.0"
edition = "2021"
description = "Procedural macros for durust: #[workflow] auto-registration."
license = "MIT OR Apache-2.0"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
